import 'package:flutter/material.dart';

class PokemonStatsGraph extends StatelessWidget {
  final List<int> stats;

  const PokemonStatsGraph({
    required this.stats,
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
