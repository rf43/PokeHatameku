import 'package:flutter/material.dart';

class PokemonStatsNumbers extends StatelessWidget {
  final List<int> stats;

  const PokemonStatsNumbers({
    required this.stats,
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
